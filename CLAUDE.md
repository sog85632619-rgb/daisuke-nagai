# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code / Codex 向けのプロジェクト概要・技術構成・開発ルールをまとめたものです。

運用方針: **Claude Code がこのファイルの内容(概要・技術構成・開発ルール)を主導で作成・更新し、ユーザーがその内容をもとに Codex へ指示を出す。** Codex 側の実装作業とClaude Codeの作業内容は `work/HANDOFF.md` に記録して共有する。

## プロジェクト概要

- 名称: ボタンライトゲーム
- 内容: 円形ボタンを押すと 10% の確率で光る(赤 or 青をランダム表示)、押した回数と光った回数をカウント表示するシンプルなミニゲーム
- UI言語: 日本語

## 技術構成

- 素の HTML / CSS / JavaScript のみ。フレームワーク・ビルドツール・外部パッケージ依存なし
- サーバーサイド処理なし。ブラウザで直接開く、または `http-server` 等の簡易静的サーバーで動作確認する

### ファイル構成(重複あり・要注意)

| ファイル | 役割 |
|---|---|
| `index.html` | ゲームのHTML本体(分割構成版のエントリーポイント) |
| `style.css` | 分割構成版のスタイル |
| `script.js` | 分割構成版のロジック |
| `game.html` | 上記3ファイルの内容をすべて1ファイルにインライン化したスタンドアロン版 |

`index.html`系(3ファイル)と `game.html`(単一ファイル)は**同じゲームを別々に実装した重複コード**です。現状は自動同期の仕組みがないため、一方に変更を加えたら**もう一方にも同じ変更を手動で反映する**必要があります。重複を解消する場合(例: `game.html` を廃止し `index.html` に一本化する等)は、事前に `work/HANDOFF.md` に方針を記録してから着手してください。

## 開発ルール

1. UIの文言・コメントは日本語を基本とする。
2. 機能追加・修正は `index.html`系 と `game.html` の**両方**に反映する(上記の重複構成に対応するため)。
3. 外部ライブラリやビルドツールを新規導入する場合は、事前に `work/HANDOFF.md` に理由と影響範囲を記録し、合意を得てから行う。
4. 自動テストは未整備。変更後は必ずブラウザで実際に動作確認する(ボタン押下・光る演出・カウント表示)。
5. コミットメッセージは変更内容が分かるように簡潔に書く。
6. パスワード・APIキー・トークンなどの秘密情報はリポジトリにもコミットメッセージにも含めない。
7. コード変更が大きくなる場合は、着手前に `work/HANDOFF.md` の「Active work」に担当範囲と編集予定ファイルを記載する。

## Codex との協業について

- 共通作業台帳: `work/HANDOFF.md`
- 役割分担の基本方針:
  - **Claude Code**: プロジェクト概要・技術構成・開発ルール(本ファイル)の維持、設計方針の決定、実装方針のレビュー、Codexへの指示内容の整理
  - **Codex**: ユーザー経由で受け取った指示に基づく実装作業(コーディング、細部の修正など)
- 詳細な運用ルール(担当範囲の記録、編集の競合回避、記録すべき内容など)は `work/HANDOFF.md` の冒頭に記載する。
